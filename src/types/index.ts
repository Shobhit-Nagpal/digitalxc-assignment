export type Email = string;

export type Employee = {
  name: string;
  email: Email;
};

export type SecretSantaResult = {
  employeeName: string;
  employeeEmail: string;
  childName: string;
  childEmail: string;
};
